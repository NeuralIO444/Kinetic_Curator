// #310: the QUALITY tier switcher (FULL / BALANCED / PERF + AUTO) is cut —
// one ceiling remains and the governor sheds honestly. The tier data itself
// stays in data/quality.js: #296 builds a performer-chosen budget ceiling
// from it later, so do NOT delete the data. This component renders nothing
// until then.
export function QualityRow() {
  return null;
}
