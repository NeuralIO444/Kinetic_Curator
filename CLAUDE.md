# Claude — read this first

This repo's coding-agent contract is [`AGENTS.md`](AGENTS.md). The engine plan [`docs/ENGINE_PLAN.md`](docs/ENGINE_PLAN.md) is **closed** (spines A–G shipped — do not reopen or re-derive). The release ordering surface is [`docs/ROADMAP_V1.md`](docs/ROADMAP_V1.md) — Matt orders every stage; nothing there is assigned until he says so.
What is actually wired vs only looks wired: verify live, never assume — `LIVE` means a proven call chain from the running instrument (`docs/ROADMAP_V1.md` phase-gate rules).

**Embargo lifted 2026-09-23** (Night Migration 30/60 sign-off in [`docs/EMBARGO.md`](docs/EMBARGO.md)): Stage 1 engine/feel work is unfrozen. The deferred pile (Studio labs, chip editors, species genomes, UV scroll, HarfBuzz, DLC, shimmer sidecar, TYPE chips, showcase rows) still waits unless Matt re-orders it.

House rules: one fix per PR, CI green, battery/lint clean, selfcheck per behavior, Loop-format bodies with literal `Closes #N` lines (auto-close depends on the words), Matt merges code PRs. One-writer files (`liveLoop.mjs`, `liveResolve.mjs`, `particles.js`) — coordinate, never collide.
