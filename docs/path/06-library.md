# 06 — Library

*Costumes on a finished body. Curator proposes. You keep.*

After 01–05. Research: `dlc/pack-01-the-cuts/` (data-only, not imported by app), voices.js flagships vs stubs, persona pack, ROOM_REVIEW §5.

## What exists

- Three voiced flagships + twelve stubs + showcase row + bio-drive names + classic ORIGIN + guest chips + MY VOICES + Curator footer.
- DLC contract already written: packs are `presets.json`, **must not** wire into live UI until earned back.
- Curator / CLIP / personas can reroll looks. Nothing puts those rerolls on the downbeat.
- Loop capture, hits, SNAP already serve "keep."

## Research take

Iconic libraries are small and load-bearing (OP-1 factory tapes, HYPE's few touring looks). Shipped KC-1 chrome is a lab notebook. Replay comes from axes (voice × system × track × weather × clock × shove), not from more names on those axes.

DLC is the honest place for the notebook. Core deck stays playable in the dark.

## Plan

**Deck (core):** swarm hype murm grid ca orbit · Night Migration · Chrome Parade · Deep Water · MY VOICES · ORIGIN · SMOKE STUDY.

**Drawer / dlc/** (do not delete engines): showcase farm, bio-drive *labels*, Fujimoto, Ghost-Lineage, persona skins. Metabolism/ACCUM/Haeckel code stays in core.

**Curator:** proposes a preset id. Performer arms the chip. If we ever auto-apply, `startBeat = beatIndex+1`. No off-grid reroll.

**Authoring freeze** until T1 exists. A new named chip is DLC unless Matt names a fourth flagship.

Packs follow `pack-01-the-cuts`: data only, no `liveLoop` imports.

## Sticky test

A stranger can play the deck without a legend. Opening a DLC pack adds costumes, not knobs. A 40-minute set uses three voices and four tracks and does not feel under-fed.

## Do not

Ship pack contents onto the mosaic "just for now." Let Curator write `layoutParams` mid-bar. Build a community gallery before the body keeps time.
