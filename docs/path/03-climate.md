# 03 — Climate

*Four windows, one weather. Cables are optional. Wind is not.*

Issues: #392 F, #342/#383 tape, leftovers on #344/#345/#374. Research: `noise.js` curl2, `liveResolve` patches, KC1_LAYERS.md.

## What exists

- 4 content tracks, PATCH OFF/MOD/FIELD/FEED. MOD/FIELD/FEED **are live** (#382/#373/#370). HYPE as MOD source is inert (no organism vx). FEED hop awaits Matt's eyes (#374).
- Each swarm mints `createNoise`. `curl2` is written and unused. Mode `flow` is a sine.
- Placement displacement `nt` is a frozen seed slice.
- Tape counter / shed ladder shipped. Preflight is PR #383. No fifth track.

## Research take

The iconic sentence needs *room tone*. TD and AE have layers. Nobody has two climates in the same curl, one eating the other's glow. That only works if noise is a world object and PATCH is a dialect on top — not three separately invented forces.

TE: four is the product. TAPE FULL is the feature. Depth is legal combinations of four tracks × three patch modes × one wind, not a fifth slot.

## Plan

1. One `createNoise(projectSeed)` in the resolver. Domain offset per track.
2. Flock / murmuration / mold default `curl2`. `point` stays for scatter / HYPE.
3. Live `nt` as offset pass, not geometry-cache bust.
4. Organism `vx/vy` so HYPE can MOD.
5. Let #383 land. Do not invent a second tape.

## Sticky test

Two swarm tracks, one seed, shared ribbons under ACCUM. MOD from HYPE moves knobs. Unaffordable fifth arm says TAPE FULL and means it.

## Do not

Re-implement applyField/Feed/Mod. Noise layer type. FEED-from-curl before #374.
