## Spec

Add a global hue-rotate control: a `filter: hue-rotate(deg)` on the root SVG,
one new `layoutParams` field (default 0), and one slider in LAYOUT.

## Notes

Deliberately left out of the build order in `NEXTGEN_SPEC.md` — it doesn't
depend on or block any of the other render/engine issues and can land any
time that area is being touched. Roughly a 20-minute job. Good first issue.
