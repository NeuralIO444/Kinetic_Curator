## Context

From HashLips' `growEditionSizeTo`. Once the render pipeline ("Render
pipeline: lift quality caps...") exists, a still is one frame, so batch is a
loop: render N seeds to disk at a chosen resolution, with the JSON sidecars
already produced by the existing snapshot flow.

## Spec

Loop `renderFinal` over N seeds, writing each rasterized image + its sidecar
to disk. Useful for a print series.

Straightforward once the render pipeline lands; worthless before it — this
issue is blocked on that one.
