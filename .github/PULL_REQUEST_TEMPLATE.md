## Loop

Which leg does this PR serve — **perform**, **capture**, **learn**, or **guide**? One sentence.

## What changed

## Files touched

## What was NOT done

(Deferred scope, explicitly out of bounds, or a follow-up left for someone else.)

## QA

How to verify on the live canvas (or: why this doesn't need live QA — docs-only, pure refactor with unchanged selfcheck hashes, etc).

## Selfcheck

`npm run lint && npm run selfcheck && npm run build` — paste the result, or note which selfcheck(s) cover this change.
